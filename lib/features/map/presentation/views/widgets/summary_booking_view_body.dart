import 'package:flutter/material.dart';

class SummaryBookingViewBody extends StatelessWidget {
  const SummaryBookingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}